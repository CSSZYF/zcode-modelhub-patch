适配版本: ZCode Desktop 3.14.1（模型设置页重构 / Personal Model 架构）
补丁版本: v2.1.2
构建时间: 2026-09-21

v2.1.2 修复:
- 关闭启动时的「功能开关配置拉取」（GET /api/v1/client/configs，请求头携带持久设备号 device_mid）：
  两个灰度开关（desktop-context-prompt / renderer-action-trace）改用默认值，不再发起该请求。
  （强制更新检查走的是另一条匿名请求，保持不动。）

v2.1.1 修复:
- 面板在「弹窗内」不可点击的问题：思考档位表 / 拉取面板 / 请求头面板现在会挂载到
  所在弹窗（role=dialog）内部，避开弹窗的滚动锁（pointer-events 禁用）与焦点陷阱。
  此前在个人模型编辑弹窗里，必须先按 ESC 关掉弹窗，才能操作档位表面板。

v2.1.0（3.14.1 适配 + 上传彻底封堵）:
- 适配 3.14.1：渲染层模型编辑器组件改名（mbn→_bn / nN→Kk），锚点已重定位
- 【上传封堵】新增三处（3.14.0 版漏掉的真实上传路径）：
  · 事件上报核心（POST https://zcode.z.ai/api/v1/event/report：app_launch / app_daily_active /
    session_create 等，带 device_mid 持久设备号）→ 发送函数直接返回，不再外发
  · OTEL 链路/指标上报（阿里云 ARMS：proj-xtrace-*.log.aliyuncs.com，local-ttft /
    renderer-action-trace / agent 侧 zcode-cli-agent）→ 打包环境变量里的端点清空
  · 崩溃上报开关（remoteCrashReporterEnabled 硬编码 true）→ 改为 false，只保留本地崩溃转储
- 原有三处继续生效：ARMS RUM 初始化 / 磁盘用量调度器 / MCP 上报
- 【新功能】推理等级表格：个人模型编辑器里新增「思考档位表」按钮，
  勾选即生成推理等级列表（off/on/minimal/low/medium/high/xhigh/max/ultra），
  自定义档位自动保留；不再需要逐个手打
- 【改进】拉取模型默认勾选该渠道已选过的模型（首次拉取仍默认全选），
  再次拉取确认不会改动原有设置

v2.0.0（3.14.0 适配重制）:
- 按 3.14.0 全新架构重写：拉取模型 / 请求头模拟 / 视觉探测 / 最终态语义
- 删除持久化改用原生 zcode.deletedModels 墓碑
- 隐私：源码级关闭 ARMS RUM / 磁盘用量 / MCP 三处自动上报
- 修复：自定义 agent 启动命令（ZCODE_AGENT_SERVER_COMMAND，如 keysmith）时的
  存储初始化失败（host 侧补 supportsStorageStartup / storagePreparationEntry）
- 修复：Plugin not found: computer-use@zcode-plugins-official（安装器物化内置插件）
- 引擎补丁（可选）：全消息可编辑，锚点已适配，含语法自检
- 安装器加固：等待用户退出 ZCode（不再强杀进程）、失败零改动、安装后回读验证
- 新增 --check 模式（只做锚点体检，不写任何文件）

v1.2.1（3.13 及更早）:
- 拉取确认改为最终态语义；删除墓碑持久化
v1.2.0:
- 原子安全重制：临时文件 + 校验 + 改名；引擎补丁询问式 + 语法自检
v1.1.x:
- 请求头模拟（Claude / Codex 预设，粘性保存）；方言感知拉取
v1.0.0:
- 首个发布：拉取模型 + 视觉探测 + 选择面板
